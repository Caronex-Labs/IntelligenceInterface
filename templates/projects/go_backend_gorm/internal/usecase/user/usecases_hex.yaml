# Configuration used to generate this use case registration file
# Generated by: gohex engine
# Template: internal/usecase/{{DOMAIN}}/usecases.go.tmpl
# Domain: user
# Entity: User

layer: "use_case_registration"
domain: "user"
entity: "User"

# Use case registration configuration
registration:
  function_name: "RegisterUserUseCase"
  description: "Registers the user use case in the dependency injection container"
  package: "user"
  
  # DI registration steps
  steps:
    - type: "provide_constructor"
      description: "Register the use case implementation"
      constructor: "NewUserUseCase"
      return_type: "*UserUseCase"
      
    - type: "dynamic_field_registration"
      description: "Add use case to central UseCases struct"
      callback_name: "register_user_usecase"
      field_name: "User"
      container_type: "*usecase.UseCases"
      method: "usecase.AddField"

# Dependency injection configuration
dependency_injection:
  container: "samber/do"
  registration_pattern: "callback_based"
  supports_dynamic_fields: true
  error_handling: "panic_on_error"

# Dynamic field management
dynamic_fields:
  enabled: true
  container_struct: "usecase.UseCases"
  field_name: "User"
  field_type: "*UserUseCase"
  add_method: "usecase.AddField"
  reflection_based: true

# Template variables used during generation
template_vars:
  domain: "user"
  domain_snake: "user"
  entity: "User"
  entity_snake: "user"
  entities: "Users"
  entities_snake: "users"
  module: "go_backend_gorm"

# Generation metadata
generation:
  template_file: "internal/usecase/{{DOMAIN}}/usecases.go.tmpl"
  output_file: "internal/usecase/user/usecases.go"
  generated_at: "2024-12-20T10:30:00Z"
  generator_version: "1.0.0"
  preserve_custom_code: false  # Registration files typically don't need custom code

# Custom code regions (for code preservation)
custom_regions:
  - name: "custom_registration_logic"
    marker: "// Add custom registration logic here"
    description: "Add any custom dependency injection logic"
    location: "after_standard_registration"
